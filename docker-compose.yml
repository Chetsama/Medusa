version: "3.9"

services:
  vllm:
    build: .
    container_name: vllm
    runtime: nvidia
    ports:
      - "8000:8000"
    volumes:
      - $HOME/.cache/huggingface:/root/.cache/huggingface
      - ./models:/models
    environment:
      - NVIDIA_VISIBLE_DEVICES=0
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True,max_split_size_mb:512
    command: >
      --model unsloth/Qwen3.8-27B-GGUF:UD-Q4_K_M
      --tokenizer unsloth/Qwen3.8-27B
      --served-model-name qwen3.8-27b
      --dtype auto
      --tensor-parallel-size 1
      --max-model-len 16384
      --gpu-memory-utilization 0.92
      --max-num-batched-tokens 2048
      --max-num-seqs 2
      --enable-auto-tool-choice
      --tool-call-parser hermes
      --trust-remote-code

  # MCP Internet Server (Provides web fetch and internet search tools via SSE)
  # mcp-internet:
  #   image: node:20-slim
  #   container_name: mcp-internet
  #   command: npx -y supergateway --port 8080 --stdio "npx -y @modelcontextprotocol/server-fetch"
  #   ports:
  #     - "8080:8080"
  #   restart: unless-stopped

  # gateway:
  #   build:
  #     context: .
  #     dockerfile: dockerfile
  #   container_name: medusa-gateway
  #   working_dir: /app
  #   volumes:
  #     - .:/app
  #   environment:
  #     - PYTHONPATH=/app
  #     - VLLM_URL=http://vllm:8000
  #     - MCP_SERVER_URL=http://mcp-internet:8080/sse
  #   command: bash -c "pip install fastapi uvicorn httpx langchain-openai langgraph langchain langchain-community langchain-mcp-adapters mcp && uvicorn gateway.fastapi_router:app --host 0.0.0.0 --port 9000"
  #   ports:
  #     - "9000:9000"
  #   depends_on:
  #     - vllm
  #     - mcp-internet
